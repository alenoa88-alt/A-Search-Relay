name: Publish approved Beeper cloud image

on:
  push:
    branches:
      - codex/beeper-hostless
    paths:
      - .github/workflows/beeper-cloud-image-push.yml

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: ./beeper-hostless
          file: ./beeper-hostless/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ghcr.io/alenoa88-alt/a-search-relay/beeper-cloud:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
